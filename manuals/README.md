Put manufacturer service manual PDFs here, plus `sources.csv` with the columns `title,brand,url,file`
(`file` is the PDF's filename; if you leave it out, the title is matched to a filename).
Nothing in this folder is invented by Bike. It only reads what you add.
